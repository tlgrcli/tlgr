"""v1 command modules, moved here verbatim.

Each group is deleted from this package by its own migration PR, when its
operations are registered in `tlgr/ops/` and generated instead. Until then
they keep working exactly as they did, over the same code paths — which is
what makes the migration one group at a time rather than one big bang.
"""
